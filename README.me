PARSING PIPEX

./pipex infile "ls -l" "wc -l" outfile

av1
infile = fd1

av2
"ls -l" = cmd1 need split too stock the command = ls and the command_arg = -l

av3
"wc -l" = cmd2 and same as av2 for split

av4
outfile = fd2 
